% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{turnout_data}
\alias{turnout_data}
\title{Simulated data on income and voter turnout}
\format{
A data frame with 5,000 rows and 7 columns:
\describe{
\item{turnout}{Outcome. Voter turnout propensity.}
\item{income}{Exposure. Caused by \code{state}, \code{age}, and \code{industry}.}
\item{state}{Confounder: a common cause of \code{income} and \code{turnout}.}
\item{age}{Confounder: affects \code{income}, \code{turnout}, and \code{industry}.}
\item{polint}{Political interest. A mediator on the path
\code{income -> polint -> turnout}. Adjusting for it removes the indirect
effect and is a bad control for the total effect.}
\item{industry}{A cause of \code{income} lying on the back-door path
\code{income <- industry <- age -> turnout}. Safe to adjust for, but not
required once \code{age} is included.}
\item{elect_comp}{Election competitiveness. A neutral control on the
outcome: it affects \code{turnout} only, so adjusting for it leaves the
estimate unbiased and improves precision.}
}
}
\source{
Simulated by \code{data-raw/make_data.R}.
}
\usage{
turnout_data
}
\description{
A simulated cross-section used to demonstrate the \code{\link[=DAGassist]{DAGassist()}} workflow on
an applied question: does higher income increase voter turnout? Because the
data-generating process is known, the correct answer is known too, and the
cost of a misspecified control set can be read directly off the output.
}
\details{
Generated from \link{turnout_dag} with \code{set.seed(42)}. Income raises turnout
directly (\code{0.30}) and indirectly by raising political interest
(\code{0.50 * 0.40 = 0.20}), so the \strong{true total effect is \code{0.50}} and the
\strong{true direct effect is \code{0.30}}.

Adjusting for \verb{\{age, state\}} recovers the total effect; adjusting for
\verb{\{age, polint, state\}} recovers the direct effect. Regressing \code{turnout} on
every available covariate returns roughly \code{0.28} — close to the direct
effect, because conditioning on the mediator \code{polint} silently changes the
estimand rather than producing an obviously wrong number.
}
\seealso{
\link{turnout_dag} for the generating graph; \link{toy_data} for a smaller
example covering every causal role.
}
\keyword{datasets}
