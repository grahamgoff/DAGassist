% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdag.R
\name{pdag_robustness}
\alias{pdag_robustness}
\title{Diagnose adjustment-set and role robustness to uncertain edge directions}
\usage{
pdag_robustness(
  dag,
  exposure,
  outcome,
  uncertain_edges = NULL,
  pdag = NULL,
  formula = NULL,
  max_uncertain = 10L
)
}
\arguments{
\item{dag}{A \code{dagitty} DAG (the "root" model).}

\item{exposure, outcome}{Optional; inferred from the DAG when omitted.}

\item{uncertain_edges}{Character vector like \code{c("A -- B")} naming edges whose
direction is unknown.}

\item{pdag}{Optional \code{dagitty} PDAG; its \verb{--} edges are treated as uncertain.}

\item{formula}{Optional model formula; used to decide whether an ambiguous
covariate is actually in your specification (affects re-estimation advice).}

\item{max_uncertain}{Integer guard on the number of uncertain edges
(default 10 -> up to 1024 worlds).}
}
\value{
A \code{DAGassist_pdag_summary} object (printed as a bullet summary).
}
\description{
Given a DAG plus one or more edges whose direction is uncertain (a PDAG),
enumerate every acyclic orientation and report whether the minimal/canonical
adjustment sets or covariate roles change -- i.e., whether your estimand is
robust to that structural uncertainty.
}
\examples{
\dontshow{if (requireNamespace("dagitty", quietly = TRUE)) withAutoprint(\{ # examplesIf}
g <- dagitty::dagitty("dag { Z->X; X->Y; Z->Y; A->B; B->Y }")
dagitty::exposures(g) <- "X"; dagitty::outcomes(g) <- "Y"
pdag_robustness(g, uncertain_edges = "A -- B")
\dontshow{\}) # examplesIf}
}
