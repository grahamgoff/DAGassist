% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance_models.R
\name{balance_models}
\alias{balance_models}
\title{Balance diagnostics across a list of fitted models}
\usage{
balance_models(
  models,
  data,
  reference = 1,
  covariates = NULL,
  threshold = 0.1,
  extra_vars = NULL,
  include_outcome = FALSE
)
}
\arguments{
\item{models}{Named list of fitted models. The names label the comparisons.}

\item{data}{The shared source data frame (a superset of every model's rows).}

\item{reference}{Name or index of the model that defines the baseline sample.}

\item{covariates}{Optional character vector of covariates to assess. Default:
the union of all models' predictors present in \code{data}.}

\item{threshold}{Flag covariates with \verb{|(S)MD|} above this (default 0.1).}

\item{extra_vars}{Additional variables that drive listwise deletion (e.g. a
weight or cluster column not detected from the call).}

\item{include_outcome}{Assess the outcome variable too (default FALSE).}
}
\value{
An object of class \code{DAGassist_balance}: a list with \verb{$reference},
\verb{$comparisons} (per-comparison (S)MD tables) and \verb{$summary} (one row per
comparison: n_ref, n_cmp, n_covariates, n_flagged, pct_flagged, any_flagged).
}
\description{
Compares the complete-case analytic sample of a reference model against each
other model in a named list, flagging covariates whose sample composition
shifts (\verb{|(S)MD| > threshold}). Engine-agnostic: it reads variable names from
each model and recomputes samples on \code{data}, so it works for \code{lm}, \code{glm},
\code{feols}, \code{coxph}, weighted fits, etc. Models are never refit.
}
