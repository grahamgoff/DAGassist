% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{toy_data}
\alias{toy_data}
\title{Toy data for demonstrating causal roles}
\format{
A data frame with 2,000 rows and 7 columns:
\describe{
\item{X}{Exposure. Caused by \code{Z}.}
\item{Y}{Outcome. Caused by \code{X}, \code{M}, \code{Z}, \code{A}, and \code{B}.}
\item{Z}{Confounder: a common cause of \code{X} and \code{Y}.}
\item{M}{Mediator: lies on the path \code{X -> M -> Y}.}
\item{C}{Collider: a common descendant of \code{X} and \code{Y}.}
\item{A}{Neutral control on the outcome; affects \code{Y} only.}
\item{B}{Neutral control on the outcome; affects \code{Y} only.}
}
}
\source{
Simulated by \code{data-raw/make_data.R}.
}
\usage{
toy_data
}
\description{
A small simulated dataset in which each causal role that \code{\link[=DAGassist]{DAGassist()}}
classifies appears at least once. Generated from \link{toy_dag}, so the
correct adjustment set is known by construction: the total effect of
\code{X} on \code{Y} requires adjusting for \code{Z} alone.
}
\details{
The data-generating process is linear and Gaussian with
\code{set.seed(42)}. True coefficients are \code{X -> Y = 0.7}, \code{M -> Y = 0.6},
\code{Z -> Y = 0.3}, \code{A -> Y = 0.2}, \code{B -> Y = -0.1}.
}
\seealso{
\link{toy_dag} for the generating graph.
}
\keyword{datasets}
