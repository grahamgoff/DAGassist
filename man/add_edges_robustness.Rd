% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_edges.R
\name{add_edges_robustness}
\alias{add_edges_robustness}
\title{Diagnose robustness to added ("missing") edges}
\usage{
add_edges_robustness(dag, exposure, outcome, add_edges, formula = NULL)
}
\arguments{
\item{dag}{A \code{dagitty} DAG.}

\item{exposure, outcome}{Optional; inferred from the DAG when omitted.}

\item{add_edges}{Character vector like \code{c("Z -> Y", "X <-> Y")}. Directed
(\verb{->}, \verb{<-}) and bidirected (\verb{<->}, latent common cause) edges are supported.}

\item{formula}{Optional model formula or engine call; used only to decide
whether a role-flipping covariate is in your specification.}
}
\value{
A \code{DAGassist_addedge_summary} object.
}
\description{
Tests how the adjustment set and identification respond to edges the root DAG
assumes are absent (DAGWOOD exclusion branches). Each added edge is evaluated
as its own branch DAG (root + that edge).
}
\examples{
\dontshow{if (requireNamespace("dagitty", quietly = TRUE)) withAutoprint(\{ # examplesIf}
g <- dagitty::dagitty("dag { Z->X; X->Y }")
dagitty::exposures(g) <- "X"; dagitty::outcomes(g) <- "Y"
add_edges_robustness(g, add_edges = c("Z -> Y", "X <-> Y"))
\dontshow{\}) # examplesIf}
}
